{
  "name": "skillproof-schemas",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "generate": "npm run build && node scripts/generate-openapi.mjs",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "yaml": "^2.9.1",
    "zod": "^3.22.4",
    "zod-to-json-schema": "^3.25.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3"
  }
}
